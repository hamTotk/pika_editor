================================================================================
Rust クレート（pika 本体に静的リンクして再頒布する第三者 OSS）
  cargo-about により Windows x64 ビルドの依存グラフから機械生成（about.toml）。
================================================================================
{{#each licenses}}

================================================================================
{{{name}}}{{#if id}}（{{{id}}}）{{/if}}
  該当クレート:
{{#each used_by}}    - {{{crate.name}}} {{{crate.version}}}
{{/each}}
--------------------------------------------------------------------------------
{{{text}}}
{{/each}}
